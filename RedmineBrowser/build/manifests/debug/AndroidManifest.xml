<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:versionCode="1" android:versionName="1.0" package="de.kollode.redminebrowser">

    <uses-sdk android:minSdkVersion="15" android:targetSdkVersion="16"/>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.USE_CREDENTIALS"/>
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS"/>
    <uses-permission android:name="android.permission.AUTHENTICATE_ACCOUNTS"/>
    <uses-permission android:name="android.permission.READ_SYNC_STATS"/>
    <uses-permission android:name="android.permission.READ_SYNC_SETTINGS"/>
    <uses-permission android:name="android.permission.WRITE_SYNC_SETTINGS"/>

    <application android:allowBackup="true" android:icon="@drawable/ic_launcher" android:label="@string/app_name" android:theme="@android:style/Theme.Holo">
        <activity android:label="@string/app_name" android:name="de.kollode.redminebrowser.ProjectListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:label="@string/title_project_detail" android:name="de.kollode.redminebrowser.ProjectDetailActivity" android:parentActivityName="de.kollode.redminebrowser.ProjectListActivity">
            <meta-data android:name="android.support.PARENT_ACTIVITY" android:value=".ProjectListActivity"/>
        </activity>

        <!-- This are the configurations for the AccountManager -->
        <activity android:excludeFromRecents="true" android:exported="false" android:name="de.kollode.redminebrowser.authentication.RedmineAuthenticatorActivity"/>

        <service android:exported="false" android:name="de.kollode.redminebrowser.authentication.RedmineAuthenticatorService">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator"/>
            </intent-filter>

            <meta-data android:name="android.accounts.AccountAuthenticator" android:resource="@xml/authenticator">
            </meta-data>
        </service>

        <!-- Service to sync the redmine issues -->
        <provider android:authorities="de.kollode.redminebrowser.provider" android:exported="false" android:label="Projects" android:name="de.kollode.redminebrowser.provider.RedmineProvider" android:syncable="true"/>

        <service android:exported="true" android:name="de.kollode.redminebrowser.sync.SyncService">
            <intent-filter>
                <action android:name="android.content.SyncAdapter"/>
            </intent-filter>

            <meta-data android:name="android.content.SyncAdapter" android:resource="@xml/syncadapter"/>
        </service>

        <activity android:label="@string/title_activity_settings" android:name="de.kollode.redminebrowser.SettingsActivity" android:parentActivityName="de.kollode.redminebrowser.ProjectListActivity">
        </activity>
    </application>

</manifest>
